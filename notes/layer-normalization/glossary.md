# Glossary — Layer Normalization

Vocabulary and academic-English phrases encountered while reading this paper. One entry per term.

---

- **covariate shift** — the distribution of inputs to a layer keeps changing during training, because the previous layer's weights are also changing. Example: "Batch normalization was originally proposed to reduce covariate shift between layers." First seen: skim pass (Q1 feedback).
